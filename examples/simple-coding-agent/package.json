{
  "name": "@aos/example-simple-coding-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@aos/core": "workspace:*",
    "@aos/runtimes": "workspace:*",
    "@aos/tool-runtime": "workspace:*",
    "@aos/tools": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "rimraf": "^6.0.1"
  }
}
